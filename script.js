const hero=document.querySelector('.hero-image');window.addEventListener('scroll',()=>{hero.style.transform=`translateY(${window.scrollY*0.05}px) scale(1.08)`;document.querySelectorAll('.reveal').forEach(el=>{const r=el.getBoundingClientRect();el.style.opacity=(r.top<window.innerHeight-100)?1:.15;});});function move(d){document.querySelector('.track').scrollBy({left:d*400,behavior:'smooth'})}